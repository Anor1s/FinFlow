# Base URL of the backend API (no trailing slash).
VITE_API_URL=http://localhost:3000/api

# Exchange-rate API. Client calls `${VITE_CURRENCY_API_URL}/${from}`
# and reads `data.rates[to]` — open.er-api.com matches that shape.
VITE_CURRENCY_API_URL=https://open.er-api.com/v6/latest
